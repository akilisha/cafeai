// ============================================================
//  cafeai-security
//  AI-specific security layer — beyond traditional guardrails.
//  Covers: prompt injection detection and RAG data leakage
//  prevention, with security events for audit.
//  This is a middleware layer. Everything is composable.
// ============================================================

description = 'AI-specific security layer for CafeAI — prompt-injection detection and ' +
    'RAG data-leakage prevention, with security events for audit.'

apply from: "$rootDir/gradle/maven-central.gradle"

dependencies {

    api project(':cafeai-core')
    api project(':cafeai-guardrails')
}
