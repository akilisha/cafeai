rootProject.name = 'cafeai'

include(
    'cafeai-core',
    'cafeai-memory',
    'cafeai-rag',
    'cafeai-guardrails',
    'cafeai-observability',
    'cafeai-security',
    'cafeai-streaming',
    'cafeai-views-mustache',
    'cafeai-connect',
    'cafeai-agents',
    'cafeai-examples'
)

// ── Capstones ────────────────────────────────────────────────
// Runnable reference applications. Consume the framework via
// project() deps so API drift breaks the build. Not published.
include(
    'capstones:invoice-processor'
)
// support-desk, meridian-qualify, acme-claims — added in Phase 3 once migrated
// (they need AgentConfig.rag(), landing in Phase 2).
